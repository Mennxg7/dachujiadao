# 小厨神驾到 · 家庭点菜与冰箱食材管理小程序 · 设计方案与实施指南

> 本文先帮你把产品的四个板块、数据结构和落地路径梳理清楚，再给出"从零开始怎么做"的实操步骤。
> 默认以**微信小程序 + 微信云开发**作为技术底座（见第四节理由），如你有跨端需求可改用 uni-app/Taro（思路一致）。

---

## 一、产品定位

一句话：**帮一家人把"今天吃什么、冰箱里有什么、怎么做"三件事打通。**

核心特点：
- 用户不是陌生人，而是**同一个家庭内部**的成员。所以"家庭组"是核心组织单位，所有数据（点菜、冰箱、菜谱）都挂在某个家庭组下。
- 四个板块不是孤立的，**点菜 ↔ 菜谱 ↔ 冰箱**应该互相联动（这是产品的灵魂，见后文）。

---

## 二、四大板块功能拆解

### 1. 点菜（家庭菜单）
| 功能 | 说明 | 优先级 |
| --- | --- | --- |
| 成员点菜 | 每个成员从菜谱库点"想吃的菜"，或手动添加一道菜 | 必做 |
| 菜单汇总 | 全家点过的菜汇总成"今日/本周菜单"，显示谁点的、是否去重 | 必做 |
| 一键生成采购清单 | 对比冰箱库存，自动算出"还缺哪些食材" | 必做 |
| 状态管理 | 想吃 / 已做 状态切换，做完可归档 | 必做 |
| 智能推荐（进阶） | 根据冰箱现有食材推荐"现在能做的菜" | 选做 |

### 2. 冰箱（食材库存）
| 功能 | 说明 | 优先级 |
| --- | --- | --- |
| 增 / 删 / 改 | 食材名、分类、数量、单位、**保质期**、存放位置 | 必做 |
| 分类视图 | 按 肉蛋 / 蔬菜 / 水果 / 调料 / 主食 等分类展示 | 必做 |
| 临期提醒 | 按保质期排序，临近过期高亮提示 | 必做 |
| 搜索 | 快速查找某样食材 | 必做 |
| 消耗记录（进阶） | 记录用了多少、补货多少，画趋势 | 选做 |

### 3. 菜谱（食谱库）
| 功能 | 说明 | 优先级 |
| --- | --- | --- |
| 增 / 删 / 改 | 菜名、封面图、所需食材及用量、步骤、难度、耗时、标签 | 必做 |
| 上传图片 | 封面图、步骤图（走云存储） | 必做 |
| 分类/标签 | 家常菜 / 汤羹 / 凉菜 / 宝宝餐 / 减脂 等 | 必做 |
| 按食材反查 | 选几样食材，找出能做的菜（联动冰箱） | 选做 |
| 一键点菜 | 从菜谱详情直接加入家庭菜单 | 必做 |

### 4. 我的（家庭与成员管理）
| 功能 | 说明 | 优先级 |
| --- | --- | --- |
| 家庭成员管理 | 增删成员、昵称、头像、角色（管理员 / 普通成员） | 必做 |
| 我的家庭 | 创建 / 切换家庭组，生成邀请码让家人加入 | 必做 |
| 个人资料 | 昵称、头像 | 必做 |
| 设置 | 临期提醒开关、清空数据等 | 必做 |
| 权限（进阶） | 管理员可管理成员、清空家庭数据 | 选做 |

---

## 三、核心数据模型（建议用文档型数据库）

把数据按"家庭"隔离，是后续权限和协作的基础。

| 集合（表） | 关键字段 | 说明 |
| --- | --- | --- |
| **family** 家庭组 | `_id`, `name`, `ownerId`, `createdAt` | 一个家 = 一条记录 |
| **familyMember** 成员 | `_id`, `familyId`, `openid`, `nickname`, `avatar`, `role` | 谁在哪个家、什么身份 |
| **ingredient** 冰箱食材 | `_id`, `familyId`, `name`, `category`, `count`, `unit`, `location`, `expireDate` | 冰箱库存条目 |
| **recipe** 菜谱 | `_id`, `familyId`, `name`, `coverImg`, `difficulty`, `duration`, `tags[]`, `createdBy` | 菜谱主表 |
| **recipeIngredient** 菜谱食材 | `recipeId`, `name`, `amount`, `unit` | 一道菜需要哪些料 |
| **recipeStep** 菜谱步骤 | `recipeId`, `order`, `text`, `image` | 做法步骤 |
| **menuItem** 点菜条目 | `_id`, `familyId`, `recipeId`(可空), `name`, `orderedBy`, `status`(想吃/已做), `date` | 当天/本周点过的菜 |

> **采购清单**不用单独建表，由"菜单所需食材 − 冰箱现有库存"实时计算即可，避免数据冗余。

---

## 四、技术选型建议

| 方案 | 适合谁 | 优点 | 缺点 |
| --- | --- | --- | --- |
| **微信小程序原生 + 云开发（推荐新手）** | 想最快跑起来、不想管服务器 | 自带数据库/云存储/云函数，免运维；家庭数据隔离简单；学习曲线平缓 | 绑定微信生态 |
| **uni-app / Taro（Vue/React 语法）** | 想一套代码发微信+支付宝+抖音 | 跨端复用、组件化体验好 | 前期配置略多 |
| **小程序 + 自建后端（Node/Java + MySQL）** | 团队大、要复杂权限/统计 | 可控性最强 | 要自己买服务器、运维，成本高 |

> **新手直接选"微信云开发"**：注册小程序 → 开通云开发 → 用上面 7 张表建集合 → 写页面。一个人两周内能出 MVP。

---

## 五、实施路线图（分阶段）

### 阶段 0：准备（半天）
1. 注册微信小程序账号（mp.weixin.qq.com），拿到 AppID。
2. 安装微信开发者工具，新建项目，勾选"云开发"。
3. 在云开发控制台创建上述 7 个集合。

### 阶段 1：搭骨架（1 天）
- 用 `tabBar` 把四个板块排成底部导航：点菜 / 冰箱 / 菜谱 / 我的。
- 写好每个板块的空页面和路由。

### 阶段 2：我的 + 家庭（2 天）
- 先做"我的"：创建家庭、生成邀请码、成员列表、角色。
- 这是地基——其他三个板块都依赖 `familyId`。

### 阶段 3：冰箱（2~3 天）
- 食材的增删改查（云数据库 CRUD）。
- 分类展示 + 临期排序 + 搜索。

### 阶段 4：菜谱（3 天）
- 菜谱 CRUD + 图片上传（云存储）。
- 食材明细、步骤编辑。

### 阶段 5：点菜 + 联动（3 天，最有价值）
- 从菜谱一键点菜，汇总成家庭菜单。
- **采购清单**：菜单所需食材 − 冰箱库存 → 缺货列表。
- （选做）按冰箱食材反查能做的菜谱。

### 阶段 6：打磨与上线（持续）
- 临期推送订阅消息、空状态引导、数据校验。
- 真机调试 → 提交审核 → 发布。

---

## 六、给新手的关键建议

1. **先打通"家庭"`familyId` 再写业务**，否则后面权限和协作全要返工。
2. **MVP 砍掉所有"选做"**，先把"点菜→采购清单"这条主链路跑通，再谈智能推荐。
3. **图片一定要走云存储**，别把图塞进数据库字段，会拖垮查询。
4. **每个写操作都要带 `familyId` 过滤**，防止 A 家看到 B 家的冰箱。
5. **多用微信官方组件**（`picker`、`swiper`、`upload` 等），少造轮子，上线快。
6. **先画原型再写代码**：用墨刀/Figma 把四个页面画一遍，和家里人确认需求，比直接敲代码省一倍时间。

---

## 七、下一步你可以选

- ✅ 让我帮你**搭一个可运行的 MVP 项目骨架**（含四个 tab 页 + 云数据库集合 + 基础 CRUD 示例）。
- ✅ 针对某一板块（比如"点菜采购清单联动逻辑"）展开写详细实现。
- ✅ 出一份**高保真原型稿 / 页面交互说明**，先和产品对齐再开发。

告诉我你想从哪一步开始，我就接着往下做。
